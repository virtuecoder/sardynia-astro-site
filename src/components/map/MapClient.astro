---
// This component renders the map container only.
// The map initialization script is loaded via a <script> tag in the body
// so it only runs in the browser, not during SSR.
---

<div id="map" class="h-full w-full"></div>